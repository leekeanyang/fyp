<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:padding="16dp"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <TextView
        android:id="@+id/txtSiteName"
        android:text="Site: Royal Belum"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtVisitorCount"
        android:text="Visitors: 0"
        android:textSize="18sp"
        android:layout_marginTop="8dp" />

    <TextView
        android:id="@+id/txtEcoCapacity"
        android:text="Eco Capacity: Low"
        android:textSize="16sp"
        android:layout_marginTop="8dp" />

    <Button
        android:id="@+id/btnScanQR"
        android:text="Scan QR to Check In"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/btnManualCheckIn"
        android:text="Check In (Manual)"
        android:layout_marginTop="12dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>
