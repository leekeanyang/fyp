<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#F5F5F5">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            android:elevation="6dp"
            app:cardCornerRadius="16dp"
            app:cardBackgroundColor="#FFFFFF">

            <LinearLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp">

                <!-- Cover Image -->
                <ImageView
                    android:id="@+id/destinationImage"
                    android:layout_width="match_parent"
                    android:layout_height="200dp"
                    android:scaleType="centerCrop"
                    android:src="@drawable/placeholder_image"
                    android:contentDescription="Destination Image" />

                <!-- Title -->
                <TextView
                    android:id="@+id/destinationTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="Destination Name"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:textColor="#1B5E20" />

                <!-- Description -->
                <TextView
                    android:id="@+id/destinationDesc"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="Destination description goes here..."
                    android:textSize="16sp"
                    android:lineSpacingExtra="4dp"
                    android:textColor="#444" />

                <!-- Highlights -->
                <TextView
                    android:id="@+id/destinationHighlights"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="🌿 Activities: Example activities\n\n🌱 Eco-Value: Example eco-value"
                    android:textSize="15sp"
                    android:lineSpacingExtra="4dp"
                    android:textColor="#555" />

                <!-- Map Button -->
                <Button
                    android:id="@+id/viewOnMapBtn"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="20dp"
                    android:text="VIEW ON MAP"
                    android:backgroundTint="#1B5E20"
                    android:textColor="#FFFFFF"
                    android:padding="12dp"
                    android:textAllCaps="true" />

                <!-- Extra Button (e.g. Book Now / Learn More) -->
                <Button
                    android:id="@+id/extraActionBtn"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="BOOK NOW"
                    android:backgroundTint="#388E3C"
                    android:textColor="#FFFFFF"
                    android:padding="12dp"
                    android:textAllCaps="true" />

            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </LinearLayout>
</ScrollView>
