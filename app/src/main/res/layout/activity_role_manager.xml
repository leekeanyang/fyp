<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Current Role Display -->
    <TextView
        android:id="@+id/tvCurrentRole"
        android:text="Current Role: "
        android:textSize="20sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="12dp"/>

    <!-- Buttons for Role Change, Undo, Redo -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:weightSum="3">

        <Button
            android:id="@+id/btnSetRole"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:text="Set Role" />

        <Button
            android:id="@+id/btnUndo"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:text="Undo" />

        <Button
            android:id="@+id/btnRedo"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:text="Redo" />
    </LinearLayout>

    <!-- Undo History Label -->
    <TextView
        android:text="Undo History"
        android:textStyle="bold"
        android:paddingTop="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- Undo History RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvUndoHistory"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="#EEE" />

    <!-- Redo History Label -->
    <TextView
        android:text="Redo History"
        android:textStyle="bold"
        android:paddingTop="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- Redo History RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvRedoHistory"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="#EEE" />

</LinearLayout>
